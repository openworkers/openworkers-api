{
  "name": "openworkers-api",
  "version": "1.1.7",
  "license": "MIT",
  "module": "src/index.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/openworkers/openworkers-api.git"
  },
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "compile": "bun build --compile --minify --sourcemap server.ts --outfile dist/openworkers-api",
    "test": "bun test",
    "lint": "prettier --write .",
    "generate-types": "bun scripts/generate-types.ts"
  },
  "dependencies": {
    "@openworkers/croner-wasm": "^0.3.0",
    "aws4fetch": "^1.0.20",
    "hono": "^4.10.6",
    "nodemailer": "^7.0.12",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^7.0.4",
    "prettier": "^3.6.2",
    "zod-to-ts": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
