{
  "name": "openworkers-api",
  "version": "1.5.1",
  "license": "MIT",
  "module": "src/index.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/openworkers/openworkers-api.git"
  },
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "compile": "bun build --compile --minify --sourcemap server.ts --outfile dist/openworkers-api",
    "build:worker": "bun build --minify --target=browser worker.ts --outfile dist/worker.js",
    "test": "bun test",
    "check": "tsc --noEmit",
    "lint": "prettier --write .",
    "format": "prettier --write .",
    "generate-types": "bun scripts/generate-types.ts",
    "clean": "rimraf dist node_modules",
    "deploy:dev": "bun build:worker && ow dev worker deploy openworkers-api ./dist/worker.js",
    "deploy:local": "bun build:worker && ow local worker deploy openworkers-api ./dist/worker.js",
    "deploy:main": "bun build:worker && ow main worker deploy openworkers-api ./dist/worker.js"
  },
  "dependencies": {
    "@openworkers/croner-wasm": "^0.3.1",
    "aws4fetch": "^1.0.20",
    "hono": "^4.11.3",
    "jszip": "^3.10.1",
    "p-limit": "^7.2.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.7.4",
    "rimraf": "^6.1.2",
    "zod-to-ts": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
